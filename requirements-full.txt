# Full bundle: image + multi-ring + graph in one environment, mirroring the cluster's
# actual training image (`/sps/hyperk/zhu/CAVERNS/env/ml_image.sif`, also at
# `/sps/t2k/melbaz/env/ml_image.sif` - the only environment meant to run everything).
# Versions checked 2026-07-31 via
# `ssh cc apptainer exec .../ml_image.sif python -c 'import torch, spconv, ...'`:
# torch 2.2.2+cu121, spconv-cu121 2.3.8, torch_geometric 2.5.3,
# torch_scatter 2.1.2+pt22cu121, torch_cluster 1.6.3+pt22cu121, timm 1.0.24,
# wandb 0.24.0.
#
# If you only need one family, prefer requirements-gpu-images.txt or
# requirements-gpu-graph.txt instead - this pulls in both spconv AND torch_geometric.
#
#   pip install -r requirements-full.txt

--index-url https://download.pytorch.org/whl/cu121
--extra-index-url https://pypi.org/simple
--find-links https://data.pyg.org/whl/torch-2.2.2+cu121.html

-r requirements.txt

torch==2.2.2
spconv-cu121==2.3.8
scipy       # multi-ring Hungarian matching
torch_geometric==2.5.3
torch_scatter
torch_cluster
timm
wandb
